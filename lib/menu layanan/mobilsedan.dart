import 'package:flutter/material.dart';

class MobilsedanScreen extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text('Mobil sedan'),
      ),
    );
  }
}
